# WorkBuddy 电商短剧自动化知识库

> 来源说明：引用链接 `sph/AGL07NzDNM` 当前返回“视频解析失败”，无法核验原视频标题、文案或博主原话。本文是结合现有 WorkBuddy 电商短剧自动化、分镜策划表与 Hypit 二创流程整理的补充方案，不应标注为原视频逐字稿。

## 一、流程目标

把参考视频的创作结构转化为可编辑、可复用、可恢复的成片工程：

参考视频 → 脚本 → 分镜表 → 生图/生视频 → 配音 → 字幕 → FFmpeg 合成 → 成片与工程文件

课程复盘与二创保持独立：

- 课程复盘：输出章节、时间轴、重点、知识卡片、思维导图与报告。
- 二创工作台：输出原片理解、新脚本、人物和场景资产需求、分镜、提示词、视频配文案与可选成片。

## 二、分镜策划表

分镜表是脚本、素材生成与成片之间的中枢。每行表示一个镜头，建议使用以下字段：

```text
镜号, 时长, 场景, 景别, 人物, 动作, 产品, 台词, 字幕, 运镜, 音效, 生图提示词, 视频提示词, 参考图, 状态
```

约束：

- 镜头顺序和剧情承接必须明确。
- 人物、服装、场景和产品素材使用稳定 ID 引用。
- 台词必须绑定说话人；无法确认时标记“待确认”。
- 分镜数量根据剧情和目标模型支持的单段时长决定，不使用固定“十八帧”。
- 输出应支持人工编辑，后续阶段始终读取最新版本。

## 三、低算力成片路线

1. 使用 FFmpeg 或合法可用的下载方式获取参考素材并抽帧。
2. 反推每个镜头的主体、动作、景别、环境与光线。
3. 生成结构化分镜表。
4. 优先使用本地或免费额度生成场景、人物、产品、细节和九宫格素材。
5. 使用本地或免费 TTS 生成配音；没有真实人物音频时不得标记为已具备声音资产。
6. 根据词级或句级时间轴生成字幕并烧录。
7. 使用 FFmpeg 拼接画面、音轨、转场和 BGM。
8. 仅对产品特写、复杂动作或关键反转使用付费云端视频模型。

原则：基础处理优先本地完成，云端能力先估价、再授权、后执行。

## 四、单产品快速 MVP

- 套路分析：输入同类参考视频，提炼钩子、冲突、转折和转化结构。
- 新脚本：按 Hook → 痛点 → 方案 → 效果 → CTA 组织。
- 分镜规划：先做可验证的小规模镜头集，包含画面、台词、字幕和音效。
- 素材准备：生成提示词、人物和产品参考图需求、配音稿。
- 本地合成：完成字幕、转场、BGM 和预览成片。
- 数据复盘：记录观众流失点、有效话术和下一版修改方向。

“30 分钟完成”只能作为理想流程目标，不应作为对所有视频和模型的固定承诺。

## 五、批量复用工作台

跑通单条后，把稳定流程保存为可复用任务模板：

```text
分析参考内容 → 生成新脚本 → 生成分镜 → 生成素材清单 → 本地预合成 → 人工审核 → 可选云端增强
```

批量任务支持：

- 单个产品生成 N 条不同创意方向的初稿。
- 多个产品组成批次，并为每个任务保留独立工程目录。
- 定时生成初稿，但发布前必须人工审核。
- 队列状态使用 `pending / building / ready / failed`。
- 单项失败不清空任务，允许从最近成功阶段继续。

## 六、人工检查点

- 开头三秒是否有明确钩子。
- 信息密度、台词速度和字幕可读性是否合适。
- 旁白、人物说话人与字幕是否一致。
- 产品名、卖点、价格和功效描述是否有可靠依据。
- 极限词、绝对化承诺和高风险广告表述是否已经复核。
- AI 生成内容是否满足目标平台的标注要求。
- 不搬运原片，只复用经过抽象的结构和方法。

## 七、映射到当前项目

### 课程复盘

使用视频解析结果生成章节、重点、时间轴、知识卡片、思维导图和报告，不进入二创成片流程。

### 二创工作台

依次生成：

1. SRT/ASR 台词与多人对白校准。
2. 核心人物、人物关系、场景、剧情单元、情绪与产品出现位置。
3. 新脚本、人物造型、场景与产品资产需求。
4. 连续分镜组和九宫格规划。
5. 视频生成提示词和通用“视频配文案”。
6. 用户明确选择出片后，再进入本地预合成或云端增强。

智能成片前至少需要：

- 已确认的新脚本。
- 角色参考图；需要稳定人物时建议准备多角度素材。
- 必要的场景图和产品图。
- 可编辑分镜表。

用户不选择出片时，只交付结构、脚本、分镜、提示词和视频配文案，避免产生不必要费用。

## 八、运行模式

### Local

- FFmpeg：抽帧、音视频处理、字幕与合成。
- WhisperX 或可用 ASR：转写、时间轴与说话人辅助识别。
- OpenCV：画面分析和素材处理。
- 本地/系统 TTS：预览配音。

### Cloud

- 视频、图像和声音模型作为可替换供应商能力接入。
- 调用前展示预估费用和将要使用的服务。
- 获得用户授权后才执行付费生成。
- 云端失败时保留本地预览和全部中间工程。

## 九、失败恢复与工程保留

任何阶段失败时保留以下已生成内容：

- 原片理解与转写结果。
- 新脚本与人物设定。
- 分镜表与素材引用。
- 图像和视频提示词。
- SVML/SVRun 或等价工程文件。
- 已生成的字幕、音轨和本地预览。

界面提供“重新尝试”和“使用当前处理结果继续”，并用普通用户能理解的语言说明失败原因。不得把缺失结果伪装为已生成内容。

## 十、后端模块建议

- `storyboard.js`：分镜策划表的生成、校验、编辑与导出。
- `remixTemplate.js`：把脚本、分镜与素材引用转换为 SVML。
- `runner.js`：封装本地/云端构建命令、费用检查与结果获取。
- `queue.js`：管理任务状态、失败原因和恢复点。
- `scheduler.js`：创建批量初稿任务；默认不自动发布。

建议输入：参考视频、字幕、产品资料、人物/场景素材、目标时长和改编要求。

建议输出：分镜策划表 JSON、生图提示词、配音稿、视频提示词、视频配文案、本地合成计划与可恢复工程。
